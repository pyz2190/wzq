# 五子棋AI程序设计文档

## 项目概述

**项目名称**：五子棋AI对战程序
**开发语言**：C语言
**设计日期**：2025-11-17
**设计目标**：实现一个高效、稳定的五子棋AI，在2秒时限内做出高质量决策

---

## 1. 需求分析

### 1.1 功能需求
- ✅ 通过stdin/stdout与评测平台交互
- ✅ 支持START、PLACE、TURN、END四种指令
- ✅ 处理12×12棋盘的非空初始局面
- ✅ 实现五子棋AI决策算法

### 1.2 性能约束
| 约束项 | 限制 | 设计目标 |
|--------|------|----------|
| 单步时间 | ≤2秒 | 1.9秒（留100ms缓冲） |
| 总时间 | ≤90秒 | 88秒（留2秒缓冲） |
| 内存 | ≤350MB | ≤100MB（保守估计） |
| CPU | 单核心 | 不使用多线程 |

### 1.3 技术约束
- 必须使用C或C++语言
- 单文件提交，不超过1MB
- 输出后必须调用fflush(stdout)
- 运行在沙盒环境，无文件读写权限

---

## 2. 整体架构设计

### 2.1 模块划分

```
五子棋AI程序
├── 主控制模块 (main.c)
│   ├── 指令解析循环
│   └── 游戏状态管理
├── 棋盘模块 (Board)
│   ├── 棋盘状态存储
│   ├── 落子/悔棋操作
│   └── 胜负判断
├── 评估模块 (Evaluator)
│   ├── 棋型识别
│   ├── 单点评估
│   └── 全局评估
├── 搜索模块 (Search Engine)
│   ├── Alpha-Beta搜索
│   ├── 候选生成
│   └── 启发式排序
└── 时间管理模块 (Time Manager)
    ├── 时间追踪
    ├── 超时检测
    └── 动态时间分配
```

### 2.2 核心数据结构

```c
// 棋盘结构
typedef struct {
    int grid[12][12];      // 0:空 1:黑 2:白
    int move_count;        // 已下棋子数
} Board;

// 走法结构
typedef struct {
    int row;
    int col;
    int priority;          // 启发式优先级
} Move;

// 搜索上下文
typedef struct {
    Board* board;
    int my_side;
    int enemy_side;
    long long start_time;
    int time_limit;
} SearchContext;

// 时间管理器
typedef struct {
    long long total_time_used;
    long long current_start;
    int turn_time_limit;
    int total_time_limit;
    int turns_played;
} TimeManager;
```

---

## 3. 棋型评估系统

### 3.1 评分体系（完全原创设计）

| 棋型 | 得分 | 说明 |
|------|------|------|
| 五连 | 100000 | 获胜 |
| 活四 | 20000 | 两端开放的四连，下一步必胜 |
| 冲四 | 5000 | 一端开放的四连 |
| 活三 | 3000 | 两端开放的三连 |
| 眠三 | 800 | 一端开放的三连 |
| 活二 | 200 | 两端开放的二连 |
| 眠二 | 50 | 一端开放的二连 |
| 单子 | 10 | 孤立棋子 |

**设计理念**：
- 五连和活四的分差极大（100000 vs 20000），确保优先获胜
- 活三得分高于冲四（3000 vs 5000），但冲四更紧急
- 活二得分适中，鼓励连子但不过度

### 3.2 评估函数设计

**三层评估架构**：

1. **快速单点评估** (`evaluate_single_point`)
   - 用途：候选排序
   - 复杂度：O(1)
   - 方法：只看该点四个方向的连子情况

2. **全局局面评估** (`evaluate_board`)
   - 用途：搜索叶节点评分
   - 复杂度：O(n²)
   - 方法：扫描整个棋盘，统计所有棋型
   - 公式：`score = my_score × 1.0 - enemy_score × 0.85`
   - 理由：进攻略优于防守

3. **威胁检测** (`detect_threats`)
   - 用途：立即战术判断
   - 检测：一步成五、双活三、多冲四等

### 3.3 棋型判断逻辑

```
判断棋型(连子数n, 左开口, 右开口):
    if n >= 5:  return 五连
    if n == 4:
        if 两端都开:  return 活四
        if 一端开:    return 冲四
    if n == 3:
        if 两端都开:  return 活三
        if 一端开:    return 眠三
    if n == 2:
        if 两端都开:  return 活二
        if 一端开:    return 眠二
    return 单子
```

---

## 4. 搜索算法设计

### 4.1 搜索框架：Alpha-Beta剪枝

**算法原理**：
- 基于极小极大搜索
- 使用Alpha-Beta剪枝减少搜索节点
- 本质：零和博弈的最优策略搜索

**伪代码**：
```
alpha_beta_search(depth, alpha, beta):
    if depth == 0 or 超时:
        return evaluate_board()

    生成候选走法
    启发式排序

    for 每个候选:
        落子
        score = -alpha_beta_search(depth-1, -beta, -alpha)
        悔棋

        if score > alpha:
            alpha = score
        if alpha >= beta:
            break  // 剪枝

    return alpha
```

### 4.2 自适应深度策略（创新点）

```c
int calculate_search_depth(int move_count) {
    if (move_count < 10)  return 4;   // 开局：候选多，浅搜
    if (move_count < 80)  return 6;   // 中局：标准深度
    return 8;                          // 残局：候选少，深搜
}
```

**设计理念**：
- 开局：候选太多，深搜会超时
- 中局：平衡深度和时间
- 残局：候选变少，可以深搜

### 4.3 候选生成优化

**邻域过滤**：
- 半径：3格（比常见的2格更激进）
- 原因：五子棋可能有跳跃式进攻

**启发式排序**：
```c
priority = attack_score × 11 + defense_score × 9
```
- 攻防权重比：11:9
- 优先尝试高优先级走法，提高剪枝效率

**候选截断**：
- 只搜索前15个候选
- 避免在低价值走法上浪费时间

### 4.4 战术检测（立即判断）

**优先级**：
1. 我方一步成五 → 立即下
2. 对方一步成五 → 立即堵
3. 我方双活三 → 必胜
4. 对方双活三 → 必堵

---

## 5. 时间管理机制

### 5.1 时间分配策略

| 时间类型 | 限制 | 设计值 | 缓冲 |
|----------|------|--------|------|
| 单步时限 | 2000ms | 1900ms | 100ms |
| 总时限 | 90000ms | 88000ms | 2000ms |

### 5.2 时间检查点

```
搜索流程中的检查点：
1. 每个根候选搜索后
2. 每层递归进入时
3. 候选生成前
4. 超时立即返回当前最优解
```

### 5.3 动态时间预算

```c
int get_time_budget(TimeManager* tm) {
    long long remaining = 88000 - tm->total_time_used;

    if (remaining < 20000)  // 总时间不足20秒
        return 1000;         // 紧急模式：每步1秒
    else
        return 1900;         // 正常模式：1.9秒
}
```

### 5.4 紧急应对

当时间严重不足时：
1. 降低搜索深度到2层
2. 减少候选数量到5个
3. 使用启发式直接选择
4. 确保至少返回一个合法走法

---

## 6. 输入输出处理

### 6.1 指令处理流程

```
主循环:
    while (读取一行):
        if START:
            初始化游戏
            输出 "OK"
            fflush(stdout)

        if PLACE:
            记录对手落子

        if TURN:
            开始计时
            搜索最佳走法
            落子
            输出 "行 列"
            fflush(stdout)
            结束计时

        if END:
            结束游戏
```

### 6.2 关键注意事项

**必须调用fflush(stdout)**：
```c
printf("OK\n");
fflush(stdout);  // 否则可能被缓冲，导致超时
```

**初始四子处理**：
```c
// 每局开始固定放置
board[5][5] = 2;  // 白
board[6][6] = 2;  // 白
board[5][6] = 1;  // 黑
board[6][5] = 1;  // 黑
```

**DEBUG输出**：
```c
#ifdef DEBUG_MODE
    printf("DEBUG 调试信息\n");
    fflush(stdout);
#endif
```

### 6.3 错误处理

**走法验证**：
- 边界检查：0 ≤ row, col < 12
- 空位检查：grid[row][col] == 0
- 兜底：如果搜索失败，返回任意合法走法

---

## 7. 优化技术总结

### 7.1 搜索优化
- ✅ Alpha-Beta剪枝
- ✅ 候选启发式排序
- ✅ 自适应搜索深度
- ✅ 候选数量截断
- ✅ 立即战术检测

### 7.2 时间优化
- ✅ 多层时间检查
- ✅ 保守时间预算
- ✅ 动态时间分配
- ✅ 紧急应对机制

### 7.3 评估优化
- ✅ 快速单点评估
- ✅ 非对称攻防权重
- ✅ 威胁等级分层

---

## 8. 与参考代码的区别

| 维度 | 参考代码1 | 参考代码2 | 我的设计 |
|------|-----------|-----------|----------|
| 搜索深度 | 固定5层 | 迭代加深1-7层 | **自适应4-8层** |
| 时间限制 | 依赖系统 | 1980ms | **1900ms（更保守）** |
| 邻域半径 | 2格 | 2格 | **3格（更激进）** |
| 攻防权重 | 10:9 | 10:9 | **11:9（更重视进攻）** |
| 评分体系 | 查表 | 复杂模式 | **威胁等级分层** |
| 候选数量 | 20个 | 10个 | **15个（折中）** |
| 语言风格 | C数组 | C++类 | **C结构体** |

**创新点**：
1. 自适应深度：根据局势动态调整
2. 三层评估：快速/精确/威胁三种评估
3. 更保守的时间控制：确保不超时
4. 更激进的邻域：发现远距离威胁

---

## 9. 预期性能分析

### 9.1 时间复杂度
- 候选生成：O(144) = O(12×12)
- 单点评估：O(1)
- 全局评估：O(144)
- Alpha-Beta搜索：O(b^d)，其中b=15（候选数），d=6（深度）
- 最坏情况：15^6 ≈ 1100万节点
- 实际剪枝后：约10万-50万节点

### 9.2 空间复杂度
- 棋盘：144 bytes
- 搜索栈：深度6 × 候选15 × 结构体 ≈ 10KB
- 总计：< 1MB（远低于350MB限制）

### 9.3 预期表现
- 单步平均用时：0.5-1.5秒
- 总用时：30-60秒（远低于90秒限制）
- 搜索深度：中局6层，残局8层
- 剪枝效率：预计剪掉90%以上节点

---

## 10. 开发计划

### 10.1 实现顺序
1. ✅ 设计完成
2. ⏳ 实现棋盘模块
3. ⏳ 实现评估模块
4. ⏳ 实现搜索模块
5. ⏳ 实现时间管理
6. ⏳ 实现IO处理
7. ⏳ 集成测试
8. ⏳ 调优和调试

### 10.2 测试策略
- 单元测试：每个模块单独测试
- 集成测试：模拟平台指令
- 性能测试：时间和内存监控
- 对抗测试：与简单AI对战

### 10.3 调优方向
- 根据实际表现调整评分权重
- 根据用时调整搜索深度
- 根据效果调整候选数量
- 优化剪枝效率

---

## 11. 风险与应对

| 风险 | 可能性 | 影响 | 应对措施 |
|------|--------|------|----------|
| 超时 | 中 | 高 | 保守时间控制+紧急模式 |
| 超内存 | 低 | 高 | 控制递归深度+精简数据 |
| 程序崩溃 | 低 | 高 | 边界检查+兜底走法 |
| 评分不准 | 中 | 中 | 测试调优+实战反馈 |

---

## 12. 总结

本设计方案综合了参考代码的优点，并加入了多项创新：

**核心优势**：
1. **稳定性**：保守的时间控制，确保不超时
2. **适应性**：根据局势动态调整深度
3. **效率性**：多重优化减少搜索节点
4. **鲁棒性**：完善的错误处理和兜底机制

**设计原则**：
- 理解算法原理，避免抄袭
- 注重工程实践，确保稳定
- 平衡性能和时间，追求最优

**能够答辩的算法理解**：
1. Alpha-Beta剪枝的原理和实现
2. 启发式搜索的作用
3. 棋型评估的设计思路
4. 时间管理的策略
5. 每个设计决策的理由

---

**设计者**：Claude Code
**设计日期**：2025-11-17
**文档版本**：v1.0
